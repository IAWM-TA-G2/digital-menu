import ReservationForm from "../components/reservation/ReservationForm";

const ReservationPage = () => (
  <section>
    <h1 className="mb-4 text-3xl font-bold text-primary">Reservation de table</h1>
    <ReservationForm />
  </section>
);

export default ReservationPage;
